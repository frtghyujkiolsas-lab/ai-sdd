# AI Badge Studio Frontend

This folder contains the React prototype for the AI badge generation demo.

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.
